services:
  society-agent:
    build: .
    ports:
      - "4000:4000"
    volumes:
      # Mount entire workspace
      - .:/app
      # Exclude node_modules to use container's version
      - /app/node_modules
    env_file:
      - .env
    restart: unless-stopped
